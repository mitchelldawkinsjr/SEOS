node_modules/
package-lock.json
.DS_Store
*.log
.tmp/

# CLI bundle is regenerated by scripts/sync-cli-bundle.mjs (run on publish
# and via `npm run bundle`). Not committed to avoid drift with workflows/context.
packages/cli/assets/
