<!-- AUTO-GENERATED by npm run agent:compose — edit AGENT.md, agent-rules/, or agent-overrides/ instead -->

# My App — Repo Context for Issue Spec Generation
---

## Repository guide

Canonical entry point for all software engineering agents in this repository. **Read this file first**, then load only the rule files listed in your role manifest (`.github/agent-manifest.json`).

Repository: [`owner/repo`](https://github.com/owner/repo)

---

## Project purpose

**My App** — replace this paragraph with what the product does and who it serves.

**Business goals:** Replace with the outcomes that matter for prioritization and scope decisions.

---

## Architecture summary

- **Frontend:** Vite + React, React Router, Tailwind CSS
- **Testing:** Playwright e2e (`e2e/`), `npm run build`, `npm run test:e2e`
- **Deployment:** static build or Docker (customize for your host)

This is a **client-side SPA**. Document your state management and API client patterns here.

### Key directories

| Path | Purpose |
|------|---------|
| `src/` | React components, pages, hooks |
| `src/lib/` | Utilities, API clients, storage |
| `e2e/` | Playwright tests |

### Extension patterns

1. Add types and domain models alongside existing ones
2. Add routes in your router entry (e.g. `src/App.tsx`)
3. Match existing component and Tailwind utility patterns

Document key directories, env vars, and deployment notes in `.github/agent-rules/` as the project grows.

---

## Agent workflow (label state machine)

**Default (hands-off):** open issue → auto spec → auto implement → **you merge**.

```text
Issue opened → needs-spec → spec-added → ready → agent-working → pr-opened → (you merge)
```

### Human gates (kept on purpose)

| Gate | Why |
|------|-----|
| **Open issue** | Product intent and priority |
| **Merge PR** | Final quality and scope approval |
| **`no-agent` / `agent-manual`** | Opt out of automation |
| **`agent-failed` recovery** | Human unblocks ambiguous specs |
| **Secrets** | Security control |

### Opt-out labels

| Label | Effect |
|-------|--------|
| `no-agent` | Skip entire pipeline (no auto spec) |
| `agent-manual` | Auto spec, but you add `ready` manually |
| `[no-agent]` in title/body | Same as `no-agent` on create |

### System labels

| Label | Meaning |
|-------|---------|
| `needs-spec` | Planning — **auto** on issue open |
| `spec-added` | Spec posted |
| `ready` | Coding agent — **auto** after spec |
| `agent-working` | Cursor cloud agent running |
| `pr-opened` | Implementation PR open |
| `agent-failed` | Needs human intervention |

Disable automation: repo variables `AGENT_AUTO_SPEC_ENABLED=false`, `AGENT_AUTO_READY_ENABLED=false`.

Config: [`.github/seos.yml`](seos.yml)

---

## Agent roster

| Agent | Context file | Trigger |
|-------|--------------|---------|
| Planning | `ai-spec-context.md` | `needs-spec` label |
| Coding | `ai-implement-context.md` | `ready` label |

Composed context files are generated by `npm run agent:compose` from this guide, `agent-rules/`, and `agent-overrides/`. Do not edit composed files by hand.

---

## Commands

| Command | Purpose |
|---------|---------|
| `npm run build` | Build / typecheck |
| `npm run test:e2e` | Tests (when behavior or UI changes) |
| `npm run agent:compose` | Regenerate agent context files |
| `npm run agent:compose -- --check` | Fail CI if context drifted |

---

## Guiding principles

1. **Small, reversible PRs** — one issue, one focused diff.
2. **Repository intelligence over generic prompting** — behavior gaps become rule or guide updates.
3. **Humans own merge** — agents never merge or deploy.
4. **Lessons compound** — capture recurring mistakes under `.github/agent-knowledge/` and promote into this guide.

---

## Product and stack notes

- **Frontend:** Vite + React, React Router, Tailwind CSS
- **Testing:** Playwright e2e (`e2e/`), `npm run build`, `npm run test:e2e`
- **Deployment:** static build or Docker (customize for your host)

This is a **client-side SPA**. Document your state management and API client patterns here.

### Key directories

| Path | Purpose |
|------|---------|
| `src/` | React components, pages, hooks |
| `src/lib/` | Utilities, API clients, storage |
| `e2e/` | Playwright tests |

### Extension patterns

1. Add types and domain models alongside existing ones
2. Add routes in your router entry (e.g. `src/App.tsx`)
3. Match existing component and Tailwind utility patterns

### Key directories

| Path | Purpose |
|------|---------|
| `src/` | Application source |
| `tests/` or `e2e/` | Automated tests (if applicable) |

### Naming conventions

- Match existing file and symbol naming in the repo
- Prefer extending existing patterns over new abstractions

### Extension patterns

- New features should follow the same layering as neighboring code
- One issue = one focused change set

---

## Architecture rules

- Prefer extending existing modules and patterns over new abstractions.
- Match naming, layering, and import style of neighboring code.
- One issue = one focused change set; do not refactor unrelated code.
- Call out migrations, new routes, schema changes, or public API changes explicitly in the spec.
- If the change is ambiguous or spans multiple subsystems, say so in Notes and recommend human review before implement.

---

You are a technical spec assistant for the **My App** repository ([`owner/repo`](https://github.com/owner/repo)).

Your job: read a GitHub issue and produce a **paste-ready GitHub comment** with repo-aligned acceptance criteria, task breakdown, and fix directions. Do not invent unrelated features.

---

## Required Output Format

Produce a GitHub-flavored markdown comment with exactly these sections:

### Acceptance Criteria

- Bullet list, specific and testable
- Reference actual file paths, type names, and patterns from this repo
- Include edge cases implied by the issue
- Use `- [ ]` checkbox syntax for each criterion

### Task Breakdown

Structured implementation checklist using `- [ ]` items. Include only tasks relevant to this issue; add sub-bullets with file paths where helpful.

### Potential Fix Directions

- Bullet list tied to likely implementation points (types, modules, routes, components, migrations)
- Not generic advice — name the files and patterns to follow

### Notes from Issue / Images

- Brief bullets referencing issue text or screenshots
- If no images, say so
- Call out ambiguities or scope boundaries from the issue body

**Rules:**

- Do not invent features not described in the issue
- Keep criteria testable — a reviewer should be able to check each box
- Match existing naming conventions in the repo
